server:
  port: 8885
  
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/IronFist9/microsrevice-config-server
          clone-on-start: true
